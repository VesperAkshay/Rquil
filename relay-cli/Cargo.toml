[package]
name = "relay-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
relay-core = { version = "0.1.0", path = "../relay-core" }
walkdir = "2.5.0"

[dev-dependencies]
tempfile = "3.27.0"
